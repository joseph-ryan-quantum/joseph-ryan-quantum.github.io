name: Sync CV from Overleaf

on:
  repository_dispatch:
    types: [cv-updated]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Pull latest CV from Overleaf-synced repo and push
        env:
          CV_REPO_TOKEN: ${{ secrets.CV_REPO_TOKEN }}
        run: |
          git remote set-url origin "https://x-access-token:${CV_REPO_TOKEN}@github.com/joseph-ryan-quantum/joseph-ryan-quantum.github.io.git"
          git remote add overleaf-cv "https://x-access-token:${CV_REPO_TOKEN}@github.com/joseph-ryan-quantum/CV_github.git"
          git fetch overleaf-cv main
          git subtree pull --prefix=cv overleaf-cv main --squash -m "Sync CV from Overleaf"
          git push origin main
